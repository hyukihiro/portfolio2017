import controller from '../_controller';
import message from '../_message'

class EventFire {
  constructor() {
    this.isWheel = true;

    this._handleEvents();
  }

  _handleEvents() {
    console.log(events);
    this._onWheel = document.addEventListener('')
  }
}

export default EventFire;
